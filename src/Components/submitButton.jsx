const SubmitButton = (props) => {
    return (
        <div>
            <label>
                <button {...props}>Submit</button>
            </label>
        </div>
    );
};

export default SubmitButton;